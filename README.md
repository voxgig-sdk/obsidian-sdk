# Obsidian SDK

Local REST API for Obsidian client, generated from the OpenAPI spec.

The Obsidian Local REST API with MCP plugin gives you two ways to interact with your Obsidian vault programmatically:

This is an unofficial SDK for the Local REST API for Obsidian public API, generated by Voxgig with [`@voxgig/sdkgen`](https://github.com/voxgig/sdkgen). It is not affiliated with, endorsed by, or sponsored by the upstream API provider.

Learn more about Voxgig SDKs at [voxgig.com/sdk](https://voxgig.com/sdk/).

Full documentation for this SDK: [https://voxgig-sdk.github.io/obsidian-sdk/](https://voxgig-sdk.github.io/obsidian-sdk/)

> TypeScript, Python, PHP, Golang, Ruby, Lua, C SDKs, a CLI with an interactive REPL, and an MCP server for AI agents — all generated from one OpenAPI spec by [@voxgig/sdkgen](https://github.com/voxgig/sdkgen).

> **Features:** `debug`, `idempotency`, `metrics`, `paging`, `ratelimit`, `retry`, `test`, `timeout` — opt-in,
> inactive until switched on, and configured per client. See the Features
> section of any SDK README below for what each one does.

## Entities, not endpoints

This SDK exposes the API as **9 semantic entities** that you
call directly, instead of assembling URL paths and query strings. See the [Entities](#entities) table below for the full list. Entities are
**Capitalised** to mark them as the primary surface, each with the operations they
support (`list`, `load`, `create`, `update`, `remove`, `patch`):

```ts
const client = new ObsidianSDK()
const active = await client.Active().load()
```

Thinking in entities keeps the mental model small — for people and AI agents alike —
rather than reasoning about raw HTTP routes and query parameters.

## Offline unit testing

Every SDK ships a built-in **test mode** that swaps the HTTP transport for
an in-memory mock, so your unit tests run fully offline — no server, no
network, and no credentials:

### TypeScript

```ts
// The offline mock starts EMPTY — seed it with the records the test needs.
// Shape: { entity: { <entity-name>: { <id>: <record> } } }
const client = ObsidianSDK.test({
  entity: {
    command: {
      test01: { id: 'test01' },
    },
  },
})
const commands = await client.Command().list()
// commands is an array of Command entities, populated with mock data
// — call commands[0].data() for the record itself
console.log(commands)
```

### Python

```python
client = ObsidianSDK.test()
commands = client.Command().list()
print(commands)
```

### PHP

```php
// Seed fixture data so offline calls resolve without a live server.
$client = ObsidianSDK::test([
    "entity" => ["command" => ["test01" => []]],
]);
$commands = $client->Command()->list();
```

### Golang

```go
client := sdk.Test()
result, err := client.Command(nil).List(
    nil, nil,
)
```

### Ruby

```ruby
# Seed fixture data so offline calls resolve without a live server.
client = ObsidianSDK.test({
  "entity" => { "command" => { "test01" => {} } },
})
commands = client.Command.list()
```

### Lua

```lua
local client = sdk.test()
local results, err = client:Command():list()
```

### C

```c
#include "core/api.h"

ObsidianSDK* client = test_sdk(NULL, NULL);
PNError* err = NULL;
Entity* command = obsidian_command(client, NULL);
voxgig_value* commands = command->vt->list(command, NULL, NULL, &err);
printf("%s\n", voxgig_to_json(commands));
```

## Packages

| Language | Package | Install |
| --- | --- | --- |
| TypeScript | `@voxgig-sdk/obsidian` | publish pending — [install from git tag](https://github.com/voxgig-sdk/obsidian-sdk/tags) |
| Python | `voxgig-sdk-obsidian` | publish pending — [install from git tag](https://github.com/voxgig-sdk/obsidian-sdk/tags) |
| PHP | `voxgig-sdk/obsidian` | publish pending — [install from git tag](https://github.com/voxgig-sdk/obsidian-sdk/tags) |
| Golang | `github.com/voxgig-sdk/obsidian-sdk/go` | `go get github.com/voxgig-sdk/obsidian-sdk/go@latest` |
| Ruby | `voxgig-sdk-obsidian` | publish pending — [install from git tag](https://github.com/voxgig-sdk/obsidian-sdk/tags) |
| Lua | `voxgig-sdk-obsidian` | publish pending — [install from git tag](https://github.com/voxgig-sdk/obsidian-sdk/tags) |
| C | `voxgig-sdk-obsidian` | publish pending — [install from git tag](https://github.com/voxgig-sdk/obsidian-sdk/tags) |
| Go CLI | `github.com/voxgig-sdk/obsidian-sdk/go-cli` | `go install github.com/voxgig-sdk/obsidian-sdk/go-cli/cmd/obsidian@latest` |
| Go MCP server | `github.com/voxgig-sdk/obsidian-sdk/go-mcp` | `go get github.com/voxgig-sdk/obsidian-sdk/go-mcp@latest` |

## Quickstart

### TypeScript

```ts
import { ObsidianSDK } from '@voxgig-sdk/obsidian'

const client = new ObsidianSDK({
  apikey: process.env.OBSIDIAN_APIKEY,
  // Required: this API's server URL is templated on these.
  server: {
    host: '<host>',
    port: '<port>',
  },
})

// Load active data (returns a Active)
const active = await client.Active().load()
console.log(active)
```

See the [TypeScript README](ts/README.md) for the full guide.

## Surfaces

| Surface | Path |
| --- | --- |
| **SDK** (TypeScript, Python, PHP, Golang, Ruby, Lua, C) | `ts/` `py/` `php/` `go/` `rb/` `lua/` `c/` |
| **CLI** | `go-cli/` |
| **MCP server** | `go-mcp/` |

## Use it from an AI agent (MCP)

The generated MCP server exposes every operation in this SDK as an
[MCP](https://modelcontextprotocol.io) tool that Claude, Cursor or Cline
can call directly. Build and register it:

```bash
cd go-mcp && go build -o obsidian-mcp .
```

Then add it to your agent's MCP config (Claude Desktop, Cursor, etc.):

```json
{
  "mcpServers": {
    "obsidian": {
      "command": "/abs/path/to/obsidian-mcp"
    }
  }
}
```

## Entities

The API exposes 9 entities:

| Entity | Description | API path |
| --- | --- | --- |
| **Active** | The Active entity (create, load, patch, remove, update). | `/active/` |
| **Command** | The Command entity (create, list). | `/commands/` |
| **Entity1** | The Entity1 entity (load). | `/` |
| **Mcp** | The Mcp entity (create, load). | `/mcp/` |
| **Open** | The Open entity (create). | `/open/{filename}` |
| **Search** | The Search entity (create). | `/search/` |
| **System** | The System entity (load). | `/obsidian-local-rest-api.crt` |
| **Tag** | The Tag entity (list). | `/tags/` |
| **Vault** | The Vault entity (create, list, load, patch, remove, update). | `/vault/` |

The operations available across these entities are **load**, **list**, **create**, **update**, **remove** — see each entity's
own list above for exactly which it supports.

## Quickstart in other languages

### Python

```python
import os
from obsidian_sdk import ObsidianSDK

client = ObsidianSDK({
    "apikey": os.environ.get("OBSIDIAN_APIKEY"),
    "server": {
        "host": "<host>",
        "port": "<port>",
    },
})


# Load a specific active (returns the record, raises on error)
active = client.Active().load()
print(active)
```

### PHP

```php
<?php
require_once 'obsidian_sdk.php';

$client = new ObsidianSDK([
    "apikey" => getenv("OBSIDIAN_APIKEY"),
]);


// Load a specific active (returns the ENTITY; call data_get() for the record; throws on error)
$active = $client->Active()->load();
print_r($active->data_get());
```

### Golang

```go
import sdk "github.com/voxgig-sdk/obsidian-sdk/go"

client := sdk.NewObsidianSDK(map[string]any{
    "apikey": os.Getenv("OBSIDIAN_APIKEY"),
    "server": map[string]any{
        "host": "<host>",
        "port": "<port>",
    },
})

// Load active data
active, err := client.Active(nil).Load(nil, nil)
if err != nil {
    panic(err)
}
fmt.Println(active)
```

### Ruby

```ruby
require_relative "Obsidian_sdk"

client = ObsidianSDK.new({
  "apikey" => ENV["OBSIDIAN_APIKEY"],
})


# Load a specific active (returns the ENTITY; call data_get for the record)
active = client.Active.load()
puts active
```

### Lua

```lua
local sdk = require("obsidian_sdk")

local client = sdk.new({
  apikey = os.getenv("OBSIDIAN_APIKEY"),
})


-- Load a specific active
local active, err = client:Active():load()
print(active)
```

### C

```c
#include "core/api.h"

ObsidianSDK* client = obsidian_sdk_new(cmap(1,
    "apikey", v_str(getenv("OBSIDIAN_APIKEY"))));
PNError* err = NULL;


Entity* active = obsidian_active(client, NULL);
// Load a specific active (returns the record, sets *err on failure)
voxgig_value* active_rec = active->vt->load(active, NULL, NULL, &err);
printf("%s\n", voxgig_to_json(active_rec));
```

## Direct and prepare

For endpoints the entity model doesn't cover, use the low-level methods:

- **`direct(fetchargs)`** — build and send an HTTP request in one step.
- **`prepare(fetchargs)`** — build the request without sending it.

Both accept a map with `path`, `method`, `params`, `query`,
`headers`, and `body`. See the [How-to guides](#how-to-guides) below.

## How-to guides

### Make a direct API call

When the entity interface does not cover an endpoint, use `direct`:

**TypeScript:**
```ts
const result = await client.direct({
  path: '/api/resource/{id}',
  method: 'GET',
  params: { id: 'example' },
})
if (result instanceof Error) {
  throw result
}
console.log(result.data)
```

**Python:**
```python
result = client.direct({
    "path": "/api/resource/{id}",
    "method": "GET",
    "params": {"id": "example"},
})
```

**PHP:**
```php
$result = $client->direct([
    "path" => "/api/resource/{id}",
    "method" => "GET",
    "params" => ["id" => "example"],
]);
```

**Go:**
```go
result, err := client.Direct(map[string]any{
    "path":   "/api/resource/{id}",
    "method": "GET",
    "params": map[string]any{"id": "example"},
})
if err != nil {
    panic(err)
}
fmt.Println(result)
```

**Ruby:**
```ruby
result = client.direct({
  "path" => "/api/resource/{id}",
  "method" => "GET",
  "params" => { "id" => "example" },
})
```

**Lua:**
```lua
local result, err = client:direct({
  path = "/api/resource/{id}",
  method = "GET",
  params = { id = "example" },
})
```

**C:**
```c
PNError* err = NULL;
voxgig_value* result = sdk_direct(client, cmap(3,
    "path", v_str("/api/resource/{id}"),
    "method", v_str("GET"),
    "params", cmap(1, "id", v_str("example"))), &err);
```

## Advanced

> Everyday use only needs the sections above. This explains the internals
> behind every call — relevant when writing custom features.

Every SDK call runs the same five-stage pipeline:

1. **Point** — resolve the API endpoint from the operation definition.
2. **Spec** — build the HTTP specification (URL, method, headers, body).
3. **Request** — send the HTTP request.
4. **Response** — receive and parse the response.
5. **Result** — extract the result data for the caller.

A feature hook fires at each stage (e.g. `PrePoint`, `PreSpec`,
`PreRequest`), so features can inspect or modify the pipeline without
forking the SDK.

### Features

| Feature | Purpose |
| --- | --- |
| **DebugFeature** | Request/response capture ring buffer for debugging |
| **IdempotencyFeature** | Idempotency keys for safe retries of mutating operations |
| **MetricsFeature** | Statistics capture: per-operation counters and latency |
| **PagingFeature** | Pagination signals for list operations |
| **RatelimitFeature** | Client-side rate limiting via a token bucket |
| **RetryFeature** | Automatic retry of transient failures with exponential backoff |
| **TestFeature** | In-memory mock transport for testing without a live server |
| **TimeoutFeature** | Per-request timeout with transport abort |

Pass custom features via the `extend` option at construction time.

## Customizing this SDK

This repository contains its own generator (`.sdk/`), so the SDK is
customizable without forking any upstream tool:

- **The model** (`.sdk/model/`) declares everything this project owns:
  package names, versions, active features, per-target settings. It is
  written in [aontu](https://aontu.dev), a JSON-based
  specification language designed for building ontologies: easy to edit
  by hand, and files unify rather than override, so small declarations
  compose into one model. Regeneration re-reads it every time.
- **Templates** (`.sdk/tm/`) and **components** (`.sdk/src/cmp/`) are
  the two layers of generation, copied into this repo: templates are the
  literal per-language source, components generate the API-shaped parts.
- **Regeneration merges.** By default, newly generated content is
  three-way merged into existing files, so generator updates and local
  edits usually converge without manual conflict handling. A project can
  opt for plain overwrite instead.
- **Custom features and entire custom targets** arrive through sdkgen
  packages (`voxgig-sdkgen package add`), on the same rails as the
  bundled languages, and `voxgig-sdkgen doctor` reports any drift from
  what a resync would write.

How-to: [customize and propagate templates](https://github.com/voxgig/sdkgen/blob/main/docs/how-to/customize-and-propagate-templates.md).
The full story: [voxgig.com/sdk/custom](https://voxgig.com/sdk/custom).

## Per-language documentation

- [TypeScript](ts/README.md)
- [Python](py/README.md)
- [PHP](php/README.md)
- [Golang](go/README.md)
- [Ruby](rb/README.md)
- [Lua](lua/README.md)
- [C](c/README.md)

## Upstream API

This SDK is generated from the upstream OpenAPI specification. It is an
unofficial client and is not affiliated with the API provider.

The OpenAPI spec(s) this SDK was generated from are kept in the
[`.sdk/def/`](.sdk/def/) folder.

- Upstream API: `https://{host}:{port}`

## Security

Please report security issues to security@voxgig.com. See [SECURITY.md](SECURITY.md).
Do not open public issues for suspected vulnerabilities.

---

Generated from the Local REST API for Obsidian OpenAPI spec by [@voxgig/sdkgen](https://github.com/voxgig/sdkgen).
